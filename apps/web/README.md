# Orbit Ledger Web

This directory is reserved for the premium SaaS web/PWA application defined in Phase 0.

Phase 1 intentionally establishes the monorepo foundation first so the future web app can be built on shared contracts, UI tokens, and sync-safe data models without reworking the mobile app again.
